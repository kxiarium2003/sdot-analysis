# 데이터 소스 관련 확인 사항

원본 데이터(S-DoT) 관련해서 원천 부서에 직접 확인한 내용을 기록합니다. 코드/문서에서 "왜 이렇게 처리했는가"의 근거로 인용할 수 있습니다.

---

## 2026-08-07 — 전송시간 결측 시 등록일자 대체 관련 확인

- **일시**: 2026-08-07(금) 14:09, 약 8분간
- **대상**: 원천 부서 (02-2133-1839)
- **확인 내용**: `전송시간`(IoT 센서가 값을 전송한 시각) 값이 결측이거나 사용 불가능한 경우, `등록일자`(DB 저장 시각)로 대체해서 사용해도 데이터 정합성에 문제가 없다는 답변을 유선으로 확인함.
- **적용 범위**: `metadata/sdot-schema-mapping.xlsx`의 `v2020_2022_recovered`, `v2020_2022_corrupted` 스키마 — `전송시간`이 결측/E+ 표기/더미값(`YYYYMMDDHHMM`로 파싱 시 월 또는 일이 `00`)인 경우에 한해 `등록일자`로 폴백.
- **구현**: `scripts/script22-merge-yearly-v8.py`의 `resolve_measure_time()` — 대체된 행은 `measure_time_source='reg_fallback'`으로 별도 표시해 원본 소스를 추적 가능하게 함.
- **한계**: 이 근거는 현재 유선 확인(구두)에만 의존하고 있음. 서면 회신이나 공식 문서가 있다면 이 파일에 추가해 보강할 것.

---

## 2026-09-07 — measure_time 연도 이상치 1건 발견 (원본 데이터 결함)

- **발견 경위**: 전체 459개 파일 병합(53.1M행) 후 독립 재검증 중, `sdot_2025_v8.parquet`의 날짜 범위가 다른 연도 파일과 달리 2년 넘게 벗어난 것을 확인 → 역추적.
- **원인**: `data/raw/S-DoT_2025/S-DoT_NATURE_2025.08.18-08.24.csv` (2025년 8월 파일) 안에 센서 `V02Q2300002`의 한 행이 `측정시간=2023-11-22_12:07:00`으로 기록되어 있음. 파이프라인 버그가 아니라 **원본 데이터 자체의 결함**(센서 시계 오류로 추정) — 820만 행 중 1건.
- **조치**: 삭제하지 않고 `measure_time_year_outlier` 플래그 컬럼으로 표시(파일이 대표하는 연도에서 ±1년 넘게 벗어나면 True). `scripts/script22-merge-yearly-v8.py`에 이 체크를 추가했고, 기존 산출물 7개 parquet에도 재병합 없이 컬럼만 패치함.
- **전체 스캔 결과**: 53.1M행 전체에서 이 1건 외 연도 이상치 없음.
- **마스터 데이터셋 구축 시 참고**: 이 행을 포함할지 제외할지는 후속 단계(마스터 병합)에서 `measure_time_year_outlier` 컬럼으로 필터링해 결정할 것.

---

## 2026-09-07 — 위치정보 매핑 누락(고아) 2,373행 조사

- **발견 경위**: `script23`(마스터 병합 + 위치매핑) 결과 독립 재검증 중, `latitude`가 결측인 행 2,373건(전체 53.1M행 중 0.0045%) 확인 → 원인 조사.
- **원인**: 서로 다른 센서 25개의 `sensor_id`가 위치정보 메타데이터 파일(`metadata/서울시 도시데이터 센서(S-DoT) 환경정보 설치 위치정보.xlsx`)에 현재 ID로도, 과거 ID(`변경 전 시리얼` 컬럼)로도 전혀 등재되어 있지 않음 — 파이프라인의 매핑 로직 문제가 아니라 **메타데이터 파일 자체가 해당 센서들을 아직 반영하지 못한 것**.
- **분포**: 상위 10개(`OC3CL200801`~`OC3CL200810`)가 각 ~200건씩, 총 2,020건으로 대부분을 차지. 나머지 15개 센서는 소량. 연도별로는 2025년이 2,020건으로 압도적(나머지는 2020~2023년에 소량 산재).
- **판단**: 특정 소수 센서(25개)에 국한된 문제로, 넓게 퍼진 매핑 오류가 아님. 위치 없는 행은 공간 분석(집계구 일반화 등)에서 자연히 제외되므로 급한 조치는 불필요.
- **후속 조치(선택)**: 메타데이터 파일 갱신본이 확보되면 재매핑 가능한지 확인. 지금 당장 필요한 조치는 아님.

---

## 2026-09-07 — KMA API 허브 ASOS 수집 시 발견한 API 제약/결측 표기 이슈

- **1회 요청 상한(중요, 무경고 절단)**: `kma_sfctm3.php`(ASOS 시간자료)는 `tm1`~`tm2`로 31일(744시간)을 초과하는 기간을 요청해도 에러를 반환하지 않고, 요청 구간의 **최신 쪽 31일치만 조용히 잘라서** 돌려준다(예: 1년치를 요청해도 마지막 달만 옴). 실측으로 확인(2020년 1년 요청 → 745행만 반환, 앞부분 11개월 누락). `scripts/script24-collect-kma-asos.py`는 이를 피하기 위해 반드시 달력월 단위(최대 31일)로 청킹하고, 월별 기대행수(일수×24) 대 실제행수를 대조하는 감사를 남기도록 구현함. **향후 AWS 등 다른 KMA API도 동일 계열이면 같은 제약이 있을 수 있으니 새 엔드포인트마다 반드시 먼저 상한을 실측할 것.**
- **결측값 표기의 모호성**: HM(습도)/SS(일조)/SI(일사)는 물리적으로 음수 불가능하므로 `-9`/`-9.0`/`-9.00`을 결측으로 명확히 판정해 NaN 처리함. 반면 **TA(기온)는 한겨울에 실제로 -9.0℃ 근방 값이 나올 수 있어, 원본 텍스트만으로는 결측 sentinel과 실측값을 구분할 수 없음**(KMA 원본 API의 알려진 한계로 보임, 공식 문서에 명시된 구분 규칙을 찾지 못함). 삭제/치환하지 않고 `ta_at_sentinel` 플래그 컬럼만 남겨 판단을 다음 단계로 미룸. 2020-04~2026-08 종로(108) 수집 결과 27건 해당.
- **SS/SI 결측의 대부분은 사실 "야간이라 관측 안 함"**: NaN 처리된 SS/SI 약 45%는 실제 센서 결함이 아니라 야간 시간대(일조/일사가 물리적으로 0인 시간)를 KMA가 값 0.0이 아닌 sentinel(-9)로 표기하기 때문. 원본 raw txt(`data/raw/KMA_ASOS_108/`)는 그대로 보존했으므로, 일사/일조 총량 등을 계산할 때 "야간=0" vs "야간=결측으로 제외"를 구분해서 다시 처리할 수 있음. 지금 단계에서는 결정하지 않고 caveat만 기록.
- **실제 결측 2개월 발견**: 종로(108) 수집 감사 로그(`docs/kma-asos-collection-audit.csv`) 기준 2023-12(744 기대 중 727행, 17시간 결측)와 2024-03(744 기대 중 726행, 18시간 결측)에서 API가 애초에 그만큼만 반환함 — 위 상한 절단 이슈와는 무관(다른 75개월은 정확히 일치했으므로 청킹 로직 문제 아님), 관측소 측 실제 결측으로 판단. 원인 불명, 급한 조치 불필요.

---

## 2026-09-07 — S-DoT 센서 측정값 품질 진단 (Phase 0 체크리스트 5번)

- **배경**: 지금까지는 measure_time 등 시간 컬럼만 검증했고 기온 등 측정값 자체는 검증한 적이 없었음. scripts/script26-diagnose-sensor-quality.py로 처음 진단. 정의/임계값은 결과를 보기 전에 docs/preregistration.md 3절에 먼저 확정함(커버리지 80퍼센트 이상/50~80퍼센트/50퍼센트 미만, 물리 타당성 [-25,45]도씨 위반 0건/1퍼센트 미만/1퍼센트 이상, 고착 없음/3~7일/7일 이상 순서로 정상/의심/불량).
- **결과**: 전체 1,200개 센서 중 정상 469 / 의심 481 / 불량 250. 리포트 전체는 docs/sdot-sensor-quality-report.csv(센서별 1행)에 저장. 원본 마스터 parquet은 수정하지 않음(등급은 참고용 진단 결과일 뿐, flag, don't filter 원칙 유지).
  - 의심(481) 대부분(449건)은 물리적 이상이 아니라 관측기간 대비 커버리지가 50~80퍼센트인 경우 - 즉 결측이 아니라 그 센서가 살아있던 기간 대비 값이 듬성듬성인 정도로, 심각한 이상은 아닐 가능성이 높음.
  - 불량(250) 중 물리 타당성 위반 171건, 고착값 139건, 커버리지 50퍼센트 미만 83건(중복 가능 - 한 센서가 여러 사유로 불량일 수 있음).
- **핵심 발견 - 기온 -40.0도씨 고착값이 지배적 패턴**: 72시간 이상 고착 구간 872건 중 831건(95퍼센트)이 정확히 -40.0에서 발생, 214개 센서·452,746회(전체 유효 기온값의 0.99퍼센트) 등장. -40.0은 물리 범위([-25,45]) 밖이라 물리 타당성 체크에도 동시에 걸림 - 두 지표가 같은 근본 원인(센서 오류 시 -40.0을 반환하는 것으로 추정되는 장치 sentinel)을 서로 다른 각도에서 잡아낸 것으로, 진단 방법론이 의도대로 작동함을 보여줌.
  - 여러 센서가 동일한 기간에 집단으로 -40.0 고착: OC3CL2000xx 계열 54개 센서가 2023-07-10~17(약 7일), 22개 센서가 2024-06-04~07-17(약 43일), 10개 센서가 2026-07-16~08-30(약 46일, 데이터 마지막 날까지 지속 - 현재도 진행 중일 가능성)에 집단으로 고착. 여러 센서가 같은 시기에 동시에 영향받는 패턴은 개별 센서 하드웨어 결함이라기보다 특정 기종/배치 또는 통신·서버 측 장애를 시사.
- **부가 발견 - 물리 범위 안이라 물리 타당성 체크로는 못 잡는 사례**: V02Q1940274/466/503/564/686 5개 센서가 2020-09-01~10-31(61일) 동일하게 0.0으로 고착. 0.0은 [-25,45] 범위 안의 있을 법한 값이라 물리 타당성 체크만으로는 걸러지지 않고, 고착값 체크로만 검출됨 - 두 체크를 모두 둔 이유가 실제로 확인된 사례.
- **참고**: 유효 기온값이 전무한 센서 6개(OC3CL200001, OC3CL200315, OC3CL200320, OT3CL110000, V02Q1940936, V02Q1950001) 중 3개(OC3CL200001, V02Q1940936, V02Q1950001)는 앞서 발견한 위치정보 메타데이터 미등재 25개 센서와 겹침 - 온보딩이 끝나지 않은 센서일 가능성.
- **후속 조치**: 급한 조치 불필요(등급표는 참고 자료). -40.0 sentinel의 정확한 의미(장치 오류 코드인지 등)는 원천 부서 확인 시 함께 물어볼 수 있는 항목으로 메모.
